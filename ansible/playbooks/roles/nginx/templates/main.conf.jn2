server {
    listen {{ nginx_port }};
    server_name {{ nginx_server_name }};

    root /var/www/html;
    index index.html;

    location {{ nginx_index_location }} {
        try_files $uri $uri/ =404;
    }
}